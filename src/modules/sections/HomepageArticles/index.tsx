import { SectionHeader } from 'modules/blocks/sectionHeader';

export const HomepageArticles = () => {
  return (
    <section>
      <SectionHeader
        heading=" Top React Post"
        lineColor="text-ui-purple"
        headingColor="text-ui-purple"
      />
    </section>
  );
};
